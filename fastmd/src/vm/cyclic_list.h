#pragma once

namespace fastmd {
    namespace detail {
        template<typename T, template<typename> typename List>
        class def_cyclic_list_impl {
        public:
            ANY
        };
    }
}